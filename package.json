{
    "name": "naco-portal-fullstack",
    "version": "1.0.0",
    "scripts": {
      "postinstall": "npm --prefix client install && npm --prefix server install && npm run build --prefix client",
      "start": "node server/index.js"
    },
   "engines": {
    "node": "22.x"
  }
}
  