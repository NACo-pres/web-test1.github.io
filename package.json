{
    "name": "nacoportal-fullstack",
    "version": "1.0.0",
    "scripts": {
      "build": "bash build.sh",
      "start": "node server/index.js",
      "postinstall": "export NODE_OPTIONS=--max-old-space-size=4096 && npm --prefix ./client install && npm --prefix ./client run build"
    },
    "engines": {
      "node": "22.x"
    }
  }
  